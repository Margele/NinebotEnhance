# 更新日志

## 未发布

- 画面提供方式改为三选一：投屏、虚拟显示器、绘制。「授权方式」顶部先选画面提供方式；虚拟显示器仅 Android 14+ 可选（低版本选中提示「仅限 Android 14+ 可用」）并需 Root 或 Shizuku / Sui，投屏和绘制不需要授权。授权选项去掉「自动」和「刷新服务状态」按钮，只显示所选后端的状态，「不降权」下加了说明。
- 新增「绘制」：模块自己把速度表和 1–3 项信息（电压 / 功率 / 速度 / 胎压 / BMS 电量）画到仪表整帧；设置里的「绘制管理」设表程（30–200 km/h）和信息项，带预览。
- 设置里「控件管理」「触摸屏管理」「启动应用」只在虚拟显示器方式显示；投屏方式不再叠加控件，画面原样送到仪表。
- 最低 Android 版本从 14 放宽到 11（API 30）：投屏和绘制可用，虚拟显示器仍需 Android 14；Android 11 上大灯 / BMS 用旧的蓝牙权限加精确位置权限。
- 设置页的「自启动设置」改为「权限设置」，所有系统都显示，点击打开模块本体的权限检测页。
- 模块本体改为权限检测页：逐行列出模块依赖的授权、当前状态和用途，未授权的行点击即申请或跳到系统页面；原来的自启动设置、通知使用权、打开九号出行按钮去掉。底部新增「强制退出九号出行」，有 Shizuku / Sui 或 Root 时直接结束九号，否则打开九号的应用信息页。
- 更新提示改用模块自己的弹窗，两个按钮：「关闭」和「打开 Release 页」；去掉「忽略此版本」。「关于」新增「检查更新」按钮，立即查询一次并提示结果。
- 发布流程改为按标签：推送 `main` 只构建，推送 `v<版本>` 标签才发布 Release。

## 1.1.13

- 车辆信息页底部一行兼容家庭账号：文案变成车名（「xxx的九号电自xxx」）时同样可以进设置，九号自己已经接管点击的改为长按进入。来自 yanjie233 的 PR #4。

## 1.1.12

- BMS 支持五种保护板协议：管理页新增“协议”，可选自动 / DL / ANT / JBD / JK / 彦阳，默认自动（按广播名和它暴露的服务识别）。DL 仍走原有密钥交换，其余四种由模块直接按各自协议读取，全部只读、不写板子设置；ANT 状态帧校验 CRC，校验不过的帧直接丢弃并重新同步。来自 yanjie233 的 PR #4，ANT 在硬件上实测过，JBD / JK / 彦阳待实测。

## 1.1.11

- 放行九号出行 6.10.12（610124146）：资源、布局和 id 与 6.10.11 一致；该版本业务 dex 由网易加固包裹，`cn.ninebot.*` 的类和成员钩子目标改由运行时 `HookCatalog.verify` 现场核对，缺失时按老规则自动关闭画面替换。

## 1.1.10

- 大灯控制新增三种控制器：蓝牙电调（摩灯客大灯电调，按百分比定位）、摩灯客卷帘控制器（30 档定位，高度取设备上报）和 SG 灯控（无定位，按「点动时长」向上或向下运行后停止）；大灯界面按类型显示密码、档位数或点动时长。
- 「保持 DPI」恢复两条实现：默认由辅助进程通过 WindowManager 强制副屏的逻辑尺寸和密度，由系统缩回缓冲区；系统拒绝时本次投屏按缓冲区尺寸继续、日志记 `RENDER fallback`，并自动勾上「设置覆盖」里的「兼容缩放」，从下次投屏起改为按渲染尺寸建屏、采集时缩回；「兼容缩放」也可手动勾选。

## 1.1.9

- 授权方式新增「不降权」：勾选后 Root 辅助进程保持 root 身份（uid 0）运行，不再 `su 2000`；以 Root 运行的 Shizuku / Sui 同样不降权。给 HyperOS 这类不给 shell `INJECT_EVENTS` 权限、投屏画面正常但副屏无法触摸的系统用；ADB 方式的 Shizuku 不受影响。日志新增 `DAEMON uid=`。
- 系统拒绝触摸注入时（日志 `Injecting input events requires ... INJECT_EVENTS`），在九号里弹窗提示到开发者选项打开「USB 调试（安全设置）」，并提供直达按钮；每次运行只弹一次。
- 「显示全部应用」的提示改为：禁止投屏非地图导航应用，本功能仅用于投屏其他地图应用。投屏非地图导航应用导致的任何后果由自己承担。

## 1.1.8

- 按钮行装回九号「仪表投屏导航」卡片里 `layoutHistory` 下面，与 1.1.4 相同：卡片随蓝牙连接出现，按钮跟着卡片走，不再锚在「车辆定位」卡片上方；没连车时从页面底部「拥有爱车第 x 天」一行进设置。
- 「全屏投屏」不再检查巡航入口是否显示、可用或属于哪个页面，点击后直接调用九号原巡航入口，连接、车型能力、开机都由九号自己的巡航逻辑检查；「车辆未连接，暂不能投屏」提示去掉。
- 「显示全部应用」的提示改为：禁止投屏非地图导航应用，本功能仅用于投屏其他地图应用。投屏非地图导航应用导致的任何后果由自己承担。

## 1.1.7

- 「全屏投屏」不再按巡航入口置灰，一直可点：点击后直接调用九号原巡航入口，车辆是否开机由九号自己的巡航逻辑检查；页面上没有巡航入口时提示「车辆未连接」。

## 1.1.6

- 投屏启动应用的选择列表默认只列出高德地图、腾讯地图、百度地图（以及当前已选应用）；底部新增「显示全部应用」，点开后弹出「请勿投屏娱乐类的应用，骑车注意安全」提示，「同意」按钮固定等待 10 秒后才能点，同意后才展开全部可启动应用，可再点「只显示地图」切回。

## 1.1.5

- 车辆页的按钮行移到“车辆定位 / 最近骑行 / 总里程”卡片上方，车辆没连蓝牙时也显示；拆成虚拟显示器图标、「全屏投屏」「设置」三个按钮，样式跟随九号（无描边、15 dp 圆角），车辆未连接时「全屏投屏」置灰。
- 虚拟显示器成为独立一层：可以先启动、随时预览，投屏接上时不重建、投屏结束不关闭，直到退出九号出行才关闭；没启动就投屏会先自动启动。设置里的「本地模拟」去掉。
- 新增七寸仪表（1024×600，编码高度对齐成 608 也认）布局档案：应用区 760×496 放在 (0,48)，底部 56 行让给仪表的电量 / 续航信息带，卡片列在 x 770–960、底边 445，避开右下角的挡位 / 车速面板和 NOS 竖条；卡片不放大，驻车避让暂不可用；「设置覆盖」多了「七寸仪表」分辨率预设。五寸和半屏布局不变。
- 虚拟显示器预览的工具栏重排：去掉「通知」，「结束」改为「关闭」（只收起预览，虚拟显示器和外接触摸屏继续运行），「校准」只在绑定的触摸屏接上时出现，「横屏」改为把整个预览窗口转成横屏。
- 大灯和 BMS 的蓝牙链路只在九号界面可见、投屏进行中或模块自己的大灯 / BMS 界面打开时保持：九号退到后台、投屏结束后几秒内断开，不再交给蓝牙协议栈待命，模块进程在后台没有定时器；音量键只在链路保持期间调灯。
- 九号在后台且没有投屏时，对模块服务的状态轮询从 1 秒放慢到 3 秒。
- 「设置覆盖」的码率范围改为 100 kbps–3 Mbps，帧率范围改为 5–30 fps。
- 设置页「自启动设置」与「授权方式」「统计信息」同一行，去掉「重新连接并读取」。
- 授权方式为「无（投屏）」时，车辆页的虚拟显示器按钮和设置里的「触摸屏管理」隐藏。
- 新增更新提示：模块最多每 6 小时向 GitHub 查一次最新 Release，有新版本时在车辆页弹窗并可直接打开 Releases 页面，设置页顶部有「更新到 x.y.z」按钮；不自动下载。
- 绑定模块服务失败时提示可能是对九号启用了「隐藏应用列表」之类的功能；九号查不到模块包时明确指出，弹窗在所有系统上都显示。
- 「保持 DPI」只保留一种实现：虚拟屏直接按手机密度和放大后的逻辑尺寸创建、采集时缩回，不再用 WindowManager 强制尺寸 / 密度；「兼容缩放」复选框和自动降级去掉。

## 1.1.4

- 模块只注入九号主进程，`:pushcore` 等辅助进程不再重复绑定。
- 系统拒绝绑定模块服务时按 1、2、4、8 秒退避重试，并用 ContentProvider 调用尝试唤醒模块进程；ColorOS / OxygenOS / realme、vivo、华为 / 荣耀也显示“自启动设置”并跳到各自的自启动页面。
- 新增启动器入口 Ninebot Enhance：提供自启动设置、通知使用权和回到九号的按钮，让系统把模块列进自启动管理并视为已打开过的应用。
- 新增「触摸屏管理」：把 USB / 蓝牙外接触摸屏绑定到虚拟屏，投屏期间独占面板并把触摸注入副屏，手机屏幕不受影响；USB 面板由模块持有以免自动休眠；四个安装方向可选；「显示触点」在画面上画出按下位置；面板量程对应整帧，应用画面外的按下忽略。
- 预览工具栏新增「校准」：依次点 5 个靶点，解出面板到画面的仿射校正，保存后立即生效。

## 1.1.3

- 「编码覆盖」改名「设置覆盖」：虚拟屏宽、高、DPI 移入其中，勾选「覆盖虚拟屏」才生效；不勾选时按仪表形状取默认值（五寸 640 × 440 @160，半屏 240 × 300 @160）。深色 / 浅色背景、「保持 DPI」和「兼容缩放」一并移入。
- 新增 BMS 管理：绑定 DL 模块保护板，本地完成 secp256k1 握手并按设定间隔轮询；仪表新增最多三行、字段可选可排序的 BMS 卡片；电压、功率卡片可选「BMS 优先」；显示条件新增「BMS 已连接」。
- 大灯绑定入口移到「控件管理 → 大灯 → 管理」。
- 系统拒绝模块服务绑定（自启动被禁止）时弹窗引导到自启动设置。
- 大灯档位改为 5–15 档（默认 8），上限按上报高限减 1。

## 1.1.2

- 「保持 DPI」在系统拒绝强制显示尺寸时降级而不中断投屏。
- 新增「兼容缩放」备用路径：副屏按渲染尺寸创建、采集时缩回，失败后自动固定启用。
- 渲染尺寸透传到守护进程，副屏与采集面大小一致。

## 1.1.1

- Android 17：守护进程自建 ApplicationSharedMemory，修复启动即退出。
- 接受 240 × 320 等小尺寸投屏配置；竖向整帧使用半屏布局（卡片铺满宽度、单列、无避让）。
- 设置覆盖新增分辨率覆盖（半屏 / 五寸 / 自定义）。
- 关于页三个调试项每次运行重置。

## 1.1.0

- 首次打开设置时的开源确认；关于页显示项目地址与作者。

## 1.0.2

- 放行九号出行 6.10.11；大灯控制、手机导航上仪表、仪表主题、寄存器探测等。
